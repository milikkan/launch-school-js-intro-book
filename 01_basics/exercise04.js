// prints '510' instead of 15 because, when one of the operands of the addition operator is a String,
// JavaScript coerces the other value into a String and performs a string concatenation
console.log("5" + 10);
