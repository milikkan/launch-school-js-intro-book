// What value does the following expression evaluate to? 'foo' === 'Foo'

console.log("foo" === "Foo");

// evaluates to false because Strings are case-sensitive and case is taken into account during String comparison
